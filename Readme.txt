git add 			añade a git 
git add .			añade todo a git
git commit -m "" 		saca foto y comenta 
git config alias.NOMBRE "" 	pone alias a comandos
git log 			muestra los commit
git status			estado del git
git checkout id 		te mueves donde quieras
git checkout main		vuelve al ultimo commit
git checkout tags/nombretag	vuelve al tag donde lo llamas 
git code . 			abre vs
git dif 			compara codigo
git reset --hard id		vuelve donde pongas el id de la captura atras y adelante
git reflog             		muestra mas que los commit todo el historial
git tag				hace referencia a puntos importantes como ser las Versiones

