Configura la información del usuario para todos los respositorios locales


git config --global user.name "[name]"			Establece el nombre que estará asociado a tus commits

$ git config --global user.email "[email address]" 	Establece el e-mail que estará asociado a sus commits

https://training.github.com/downloads/es_ES/github-git-cheat-sheet/

git add 			añade a git 
git add .			añade todo a git
git commit -m "" 		saca foto y comenta 
git config alias.NOMBRE "" 	pone alias a comandos
git log 			muestra los commit
git status			estado del git
git checkout id 		te mueves donde quieras
git checkout main		vuelve al ultimo commit
git checkout tags/nombretag	vuelve al tag donde lo llamas 
git code . 			abre vs
git diff			compara codigo
git reset --hard id		vuelve donde pongas el id de la captura atras y adelante
git reflog             		muestra mas que los commit todo el historial
git tag				hace referencia a puntos importantes como ser las Versiones
git merge			combina los cambios de las ramas 
git branch nombrerama 		crea una rama 
git switch nombrebranch		cambia el main a la rama
git stash 			guarda temporalmente cambias sin hacer commit
git stash list			mustra lista de stashs
git stash pop			volver donde dejaste de forma temporal el stash
git stash drop			elimina los stash

git branch			muestra ramas 
2:02:45